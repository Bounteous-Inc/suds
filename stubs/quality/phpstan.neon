# mglaman/phpstan-drupal (Drupal-aware service/hook analysis) and
# phpstan-deprecation-rules are auto-registered by phpstan/extension-installer.
parameters:
    level: 6
    paths:
        - {{ drupal_root }}/modules/custom
        - {{ drupal_root }}/themes/custom
    excludePaths:
        - vendor
